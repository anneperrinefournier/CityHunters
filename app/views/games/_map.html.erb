<%# mt-3 à retirer sur app mobile car la map sera sur le swipe %>
    <div class="mt-3">
      <div style="width: 600px; height: 600px;"
        data-controller="game"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
