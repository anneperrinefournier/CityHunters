  <ul class="dropdown-menu">
    <li><a class="dropdown-item" href="#">Mon Profil</a></li>
    <li ><%= link_to "Se Déconnecter", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %></li>

    <li><a class="dropdown-item" href="#">Code de la partie</a></li>
    <% if defined?(@game) && @game %>
      <li data-controller= "burger" data-burger-game-id-value="<%= @game.id %>">
        <button class="dropdown-item" data-action="click->burger#endGame">Finir la partie
        </button>
      </li>
    <% end %>
  </ul>
